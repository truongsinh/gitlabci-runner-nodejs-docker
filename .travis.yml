language: ruby
#node_js
#services: mongodb
env:
  - RUN=mean
  - RUN=ruby
  - RUN=mongo
  - RUN=apt
  - RUN=ssh
  - RUN=all
  #  fast_finish: true
  global:
    - MODE_DEBUG=false 
  matrix:
    allow_failures:
      - env: RUN="test_err"
  
        
      
cache:
  - apt
  - npm
  - bundler
#  - vendor/bundle
#  - npm_modules
#  - /tmp
#  - /opt
  
script:
  - sudo apt-get -y update 1>/dev/null
  - chmod u+x travis.sh
  - ./travis.sh


#after_success:
#  - sleep 5; curl 0.0.0.0:3000

language: ruby
#node_js
#services: mongodb
env:
  global:
    - MODE_DEBUG=false 
  matrix:
 #   - CI_NODE_INDEX=0
    - RUN="mean"
    - RUN="test_err"
  
    
cache:
  - apt
  - npm
  - bundler
#  - vendor/bundle
#  - npm_modules
#  - /tmp
#  - /opt
  
script:
  - sudo apt-get -y update 1>/dev/null
  - chmod u+x once.sh
  - ./once.sh
  - chmod u+x travis.sh
  - ./travis.sh install


#after_success:
#  - sleep 5; curl 0.0.0.0:3000
